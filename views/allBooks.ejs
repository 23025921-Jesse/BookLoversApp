<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <!-- Font Awesome for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css">
  <title>BookStore App</title>
  <style>
    /* Style for the navbar */
    .navbar {
      margin-bottom: 20px;
    }

    /* Set background color of the webpage to beige */
    body {
      background-image: url('/images/background.jpg'); 
      background-size: cover;
      padding-top: 80px; 
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    
    /* Style for the footer */
    .footer {
      background-color: #333;
      color: white;
      text-align: center;
      padding: 20px 0;
      margin-top: auto; 
    }

    .footer .fas {
      margin: 0 5px; 
      font-size: 1.2rem; 
    }
  </style>
</head>
<body>
    <!-- Navigation Bar -->
    <%- include('navbar') %>

    <!-- Main Content -->
    <div class="container">
      <br>
      <div class="text-center"><h2>All the books that we offer</h2></div>
      <br>
  
      <!-- Books Table -->
      <table class="table table-hover table-bordered small text-center">
        <thead>
          <tr>
            <th width="100">Book Name</th>
            <th width="100">Book Image</th>
            <th width="50">Author</th>
            <th width="100">Actions</th> 
          </tr>
        </thead>
        <tbody>
          <% for(let i=0; i < books.length; i++) { %> 
            <tr>
              <td><a href="/books/<%= books[i].bookId %>"><%= books[i].name %></a></td>
              <td><img src="images/<%= books[i].image %>" width="50" height="50"></td>
              <td><%= books[i].author %></td>
              <td>
                <form action="/delete-book/<%= books[i].bookId %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this book?');">
                  <button type="submit" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i> Delete</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>

    <!-- Footer -->
    <%- include('footer') %>
</body>
</html>
